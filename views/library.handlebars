<section class="container">
  <h3 class="library-name">{{library.name}}</h3>
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each library.blogs as |blog|}}
    {{> blog-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}